				<%= form_with model:@post_comment, url:book_post_comments_path(book.id) do |f| %>
				<%= f.text_area :comment, rows:'5', class: "form-control",placeholder: "コメントをここに" %>
				<%= f.submit "送信する", class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
				<% end %>

				<td> <p>commnets</p>
					<span><% book.post_comments.count %></span>
					<h4 class="comment-count"><%= book.post_comments.count %>件コメント</h4></td>
				</tr>
			</table>
			<table>
				<thead>
					<tr>
					<th>User</th>
					<th>commnet</th>
					<th>Posted at</th>
					<th></th>
				</tr>
				</thead>
				<tr>
				<% book.post_comments.each do |c| %>
				<td><%= c.user.name %></td>
				<td><%= c.comment %></td>
				<td><%= c.created_at.strftime('%Y/%m/%d') %></td>
			</tr>
				<% if c.user == current_user %>
				<td><%= link_to "削除",  book_post_comment_path(c.book_id, c.id), method: :delete, remote: true %></td>
					</tr>
				<% end %>
				<% end %>
			</table>
